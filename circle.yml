machine:
  node:
    version: 4.2.6

general:
  branches:
    only:
      - master

dependencies:
  override:
    - npm install npm@latest -g
    - npm install angular-cli@1.0.0-beta.22-1 -g
    - npm install

test:
  override:
    #the following commands deploy to the beta.capsulecd.com domain.
    - ng github-pages:deploy --message "Autodeploy gh-pages" --base-href "/"